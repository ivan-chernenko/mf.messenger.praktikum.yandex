export interface InputProps {
    title: string;
    inputClassName: string;
    labelClassName: string;
    placeholder: string;
    type: string;
    name: string;
    hasError?: boolean;
    value?: string;
}